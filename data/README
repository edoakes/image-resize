OL tests are install cache only (denoted by 'nocache') and install + import cache ('denoted by cache').

Each test sends a single request to resize 'n' copies of the test image in the parent directory.
